# 软件工程课程第二次个人作业：利用 AIGC 完成“一箭又一箭”小游戏

| 项目 | 内容 |
|---|---|
| 这个作业属于哪个课程 | [2026-01 软件工程和软件工程实践](https://edu.cnblogs.com/campus/fzu/2026-01SoftwareEngineeringandSoftwareEngineeringPractice) |
| 这个作业要求在哪里 | [软件工程课程第二次个人作业](https://edu.cnblogs.com/campus/fzu/2026-01SoftwareEngineeringandSoftwareEngineeringPractice/homework/16718) |
| 这个作业的目标 | 使用 Python 和 AIGC 完成“一箭又一箭”小游戏 |
| 学号 | 102402150 |
| GitHub 仓库 | [github.com/zzq1419/arrow-arrow](https://github.com/zzq1419/arrow-arrow) |

> 本文只使用自己编写的关卡数据、绘制代码和规则代码，没有使用原商业游戏的代码、
> 美术素材、音效或关卡。全文的代码片段都来自仓库里的 `arrow_game.py`。
>
> 文中图片对应仓库 `assets/` 目录，发布时上传为博客图片即可。

## 一、项目展示

项目名称：**一箭又一箭**

这是一个点击式箭头解谜小游戏。棋盘上散布着许多带方向的箭头，玩家要一边观察
箭头之间的阻挡关系，一边按合适的顺序把箭头一支支“放走”。

运行方式（Windows 直接双击 `run_python_game.bat` 也可以）：

```powershell
python -m pip install -r requirements.txt
python arrow_game.py
```

### 1. 开始界面

开始界面显示游戏标题、当前选中的关卡，以及“开始游戏 / 选择关卡 / 历史得分”三个入口。
背景是缓缓向下流动的箭头底纹（用 `pygame.draw` 画出来的，没有用任何图片素材）。

![开始界面](assets/home.png)

### 2. 游戏界面

游戏界面顶部是关卡号、剩余箭头数、剩余生命（三颗爱心）、倒计时和“重玩”按钮；
中间是棋盘；底部是“提示”、缩放滑杆和“辅助线”开关。
截图里顶部那条深色提示条就是**碰撞反馈**：点击了一支被挡住的箭头，生命值从 3 变成 2。

![游戏界面](assets/game.png)

### 3. 通关界面

清空最后一支箭头后弹出通关结果，显示用时、剩余生命和通关得分，并提供
“回到主界面 / 挑战下一关 / 选择关卡”三个按钮。

![通关界面](assets/success.png)

### 4. 失败界面

三次误点之后生命值耗尽，弹出失败结果，可以直接重玩当前关卡或者回到主界面。

![失败界面](assets/failure.png)

### 5. 其他界面

| 选择关卡 | 历史得分 |
|---|---|
| ![选择关卡](assets/level-select.png) | ![历史得分](assets/history.png) |

## 二、项目介绍

### 1. 游戏规则

棋盘是 18 列 × 28 行的网格，每支箭头占据**若干个连续格子**：箭身像一条折线，
最后一格是头格，头格的朝向是上、下、左、右四个方向之一。

点击一支箭头后，程序从它头格的相邻格开始，沿箭头朝向一格一格检查到棋盘边界：

- 路径上**没有**其他箭头（也不能撞到自己的尾巴），箭头沿着朝向飞出棋盘并消失；
- 路径上**有**其他箭头，箭头留在原地不动，视为一次失误，生命值 -1，并在顶部
  给出提示文字；
- 生命值共 3 点，归零则本关失败；
- 清空棋盘上全部箭头即通关，可以进入下一关。

### 2. 界面设计

界面用 `pygame.draw` 从零绘制，没有引入任何图片文件，所以仓库里不需要额外的
美术资源。视觉上做了这些取舍：

- 棋盘区是一块独立的画布，先按 3 倍分辨率渲染再做 `smoothscale` 缩小，
  这样缩放窗口时线条依然平滑；
- 箭头本身是非自交的折线，圆角用“在拐点画圆”补齐，箭尖用三角形绘制；
- 底部固定工具条（提示、缩放、辅助线），避免功能按钮和棋盘抢位置；
- 提供日间 / 夜间两套配色，夜间模式是默认值。

### 3. 主要功能

| 功能 | 说明 |
|---|---|
| 开始界面 | 标题、当前关卡、开始游戏 / 选择关卡 / 历史得分 |
| 游戏界面 | 关卡号、棋盘、剩余箭头数、剩余生命、倒计时、重玩按钮 |
| 路径检测 | 判断头格到棋盘边界之间是否存在阻挡 |
| 飞出动画 | 箭头沿自己的朝向逐格飞出棋盘，飞出后再从棋盘上移除 |
| 碰撞反馈 | 箭头原地不动 + 顶部提示条 + 生命值 -1 |
| 失败与重开 | 生命归零显示失败弹窗，重玩按钮恢复布局和生命 |
| 通关流程 | 清空箭头显示结果，可进入下一关（第 5 关之后回到第 1 关） |
| 关卡选择 | 五关任选，关卡按难度递增 |
| 提示 | 高亮一支当前一定能飞出的箭头 |
| 辅助线 | 显示网格线，方便判断箭头是不是在同一行 / 列 |
| 缩放与拖动 | 0.75 ~ 1.25 倍缩放，放大后可拖动查看 |
| 日间 / 夜间模式 | 一键切换配色 |
| 计分与历史 | 按剩余时间和剩余生命折算成百分制得分，通关记录写入本地 |

### 4. 与作业基础要求的对应

作业的基础版要求“网格中的单格箭头”。本项目保留了原游戏更接近的形态：
一支箭头占多个格子，头格决定朝向。**这是单格规则的自然推广**——单格箭头只是
“箭身长度为 1”的特例。多出来的地方只有一处：一支箭头自己的尾巴如果正好落在
头格前方，也会挡住自己，这既符合直觉，也让关卡有了更多花样。除此之外路径检测
的判断方式与作业完全一致：仍然只看同一行或同一列、只看箭头与边界之间有没有东西。

## 三、实现思路

### 1. 箭头和方向怎么表示

坐标统一用 `(col, row)`，即 `(列, 行)`。一支箭头用一个数据类表示：

```python
@dataclass
class Arrow:
    id: int
    path: list[tuple[int, int]]   # 箭身经过的格子，最后一格是头格
    dx: int                       # 头格朝向，横向分量
    dy: int                       # 头格朝向，纵向分量
    color: tuple[int, int, int]
    progress: float = 0.0         # 飞出动画进度 0~1
    moving: bool = False
    # ... 动画相关的字段
```

四个方向直接写成 `(dx, dy)`：

| 方向 | dx | dy |
|---|---:|---:|
| 右 → | +1 | 0 |
| 左 ← | -1 | 0 |
| 下 ↓ | 0 | +1 |
| 上 ↑ | 0 | -1 |

这样“沿方向走一格”就是一次向量加法 `(col + dx, row + dy)`，四个方向不需要写四套
分支，这是我在写路径检测时最先确定的一件事。

整个关卡用一个状态类装起来：

```python
@dataclass
class GameState:
    arrows: list[Arrow]
    cols: int = 18
    rows: int = 28
    level_number: int = 1
    seconds_left: int = MAX_SECONDS
    lives: int = 3
    complete: bool = False
    dead: bool = False
    busy: bool = False            # 是否有箭头正在飞出
    started_at: int = 0
    score: dict | None = None
```

### 2. 路径检测（重点）

时钟方向上只需要关心箭头的**头格**。从“头格 + 方向”开始，一格一格往前走，
只要还没走出棋盘，就看这一格是不是被别的箭头占了：

```python
def arrow_can_leave_from_occupied(arrow, occupied, cols, rows) -> bool:
    head_col, head_row = arrow.path[-1]
    col, row = head_col + arrow.dx, head_row + arrow.dy
    while 0 <= col < cols and 0 <= row < rows:
        if (col, row) in occupied:
            return False
        col += arrow.dx
        row += arrow.dy
    return True
```

这里有三个关键点：

1. **检查的是整条路径，不是相邻一格。** `while` 循环会一直走到坐标越界为止，
   所以中间隔了十个空格子的箭头同样算阻挡；
2. **循环条件本身就是边界处理。** 当 `col` 或 `row` 越界时循环直接结束并返回
   `True`，不会访问到数组外面去——这也是 T03 想验证的行为。朝向棋盘外的箭头，
   循环体一次都不执行，直接判定可以飞出；
3. **`occupied` 里包含这支箭头自己的格子。** 所以如果它自己的尾巴在头格前方，
   同样会被判为阻挡（提示文案会说“前方是自己的尾巴，不能走”）。

为了在界面上给出“被谁挡住了”的提示，还有一个会返回阻挡者信息的版本：

```python
def get_block_info(arrows, arrow, cols, rows) -> dict:
    occupied = {}
    for owner in arrows:
        for cell in owner.cells:
            occupied[cell] = owner
    head_col, head_row = arrow.path[-1]
    col, row = head_col + arrow.dx, head_row + arrow.dy
    cells = []
    while 0 <= col < cols and 0 <= row < rows:
        if (col, row) in occupied:
            return {"blocked": True, "cells": cells, "blocker": (col, row),
                    "owner": occupied[(col, row)]}
        cells.append((col, row))
        col += arrow.dx
        row += arrow.dy
    return {"blocked": False, "cells": cells, "blocker": None, "owner": None}
```

它和上一个函数的区别是：`occupied` 从“格子集合”换成了“格子 → 箭头”的字典，
这样一旦撞上就能立刻知道是哪一支箭头挡的，可以据此区分“被自己的尾巴挡住”和
“被别的箭头挡住”两种提示。

点击处理的逻辑就变成了一次三选一：

```python
def shoot(self, arrow):
    # dead / complete 之后 update_game 不再推进动画，所以这里必须拒绝点击
    if not self.game or arrow.moving or self.game.busy \
            or self.game.complete or self.game.dead:
        return
    if not can_arrow_leave(self.game.arrows, arrow, self.game.cols, self.game.rows):
        info = get_block_info(self.game.arrows, arrow, self.game.cols, self.game.rows)
        self.lose_life()                       # 失误：生命值 -1
        if self.game.lives > 0:
            message = "前方是自己的尾巴，不能走" if info["owner"] is arrow \
                      else "前方有箭身或尾巴挡住了"
            self.set_toast(f"{message} · 生命值 -1（剩余 {self.game.lives}）")
        return
    self.game.busy = True                      # 合法：进入飞出动画
    arrow.moving = True
    arrow.progress = 0
    arrow.route, arrow.move_distance = self.build_snake_route(arrow)
    arrow.move_started = pygame.time.get_ticks()
    arrow.duration = 340 + arrow.move_distance * 24
```

### 3. 飞出动画

箭头飞出时并不是直接消失，而是“整条箭身沿着自己的朝向平移出棋盘”。做法是把
头格的路径按朝向延长若干格，拼成一条完整路线：

```python
def build_snake_route(self, arrow):
    body_length = len(arrow.path) - 1
    head_col, head_row = arrow.path[-1]
    steps = ...                                # 头格到边界的格数
    move_distance = body_length + steps + 2    # 箭身长度 + 出界距离 + 余量
    route = [(float(col), float(row)) for col, row in arrow.path]
    for step in range(1, move_distance + 1):
        route.append((head_col + arrow.dx * step, head_row + arrow.dy * step))
    return route, move_distance
```

每帧按时间比例算出 `progress`，再在 `route` 上取插值位置绘制。整条路线走完时
才把箭头从 `arrows` 里移除；如果移除后列表空了，就说明通关。

### 4. 关卡怎么来的：程序化生成 + 可解性兜底

手写关卡数据很容易写出“看起来能过、实际上死局”的布局。所以关卡不是写死的数组，
而是由一个带种子（可复现）的生成器现场造出来的，`build_level()` 分四步：

1. **基础箭头**：随机选起点和长度，沿行 / 列的固定方向生成折线，要求不重叠、
   在棋盘内，并且头格正前方一开始是空的；
2. **延长箭身**：从箭尾往外接一格，接完立刻跑一次可解性检查，不可解就把这一格退回去；
3. **填缝**：把棋盘上还没用到的“两格通路”按随机顺序往里填，同样每填一次就检查一次；
4. **兜底修复**：最后整体再检查一遍，如果还有解不开的残余箭头，就逐个删掉重新检查。

可解性检查就是一个贪心/回溯搜索——每一步找出“当前没有阻挡的箭头”移除一个，
如果某一步一个都找不到，说明这个布局是死局：

```python
def solve_removal_order(candidate):
    remaining = list(candidate)
    occupied_now = {cell for arrow in remaining for cell in arrow.cells}
    order = []
    while remaining:
        next_index = next((i for i, a in enumerate(remaining)
                           if arrow_can_leave_from_occupied(a, occupied_now, cols, rows)), -1)
        if next_index < 0:
            return False, order, remaining      # 死局
        selected = remaining.pop(next_index)
        order.append(selected)
        for cell in selected.cells:
            occupied_now.discard(cell)
    return True, order, []
```

生成器最后还会算一份“难度画像” `difficulty_profile`（每一步有多少个合法选择、
有多少步只有唯一解），用它挑选真正有思考量的布局。最终五关的箭头数量是
**58 / 79 / 87 / 99 / 136**，每一关都能通关。

### 5. 游戏状态与流程

```
开始界面 ──开始游戏──▶ 游戏界面
                          │
      ┌───────────────────┼────────────────────┐
      │                   │                    │
  点空/无效           合法点击             被阻挡点击
  （忽略）          （飞出动画）        （生命 -1 + 提示）
                          │                    │
                   棋盘清空？            生命 = 0 ？
                          │                    │
                       通关弹窗            失败弹窗
                          │                    │
                   下一关 / 回主页      重玩 / 回主页
```

重玩（`start_game()`）做的事情是重新克隆一份关卡模板：箭头布局、生命值、
倒计时、飞出状态全部回到初始值，所以“重新开始”不会把上一局的状态带进来。

## 四、AIGC 使用过程

这个项目全程使用 AIGC 编程工具协作。下面五次是有代表性的过程，都是真实发生过的，
不是项目做完之后补写的。

### 总览

| 子任务 | 借助何种 AIGC 技术 | AI 实现或提供了什么 | 效果如何 | 人工修改 |
|---|---|---|---|---|
| 项目结构与代码骨架 | AIGC 编程工具（对话式代码生成） | 给出 `Arrow` / `GameState` 数据结构、`pygame` 主循环、棋子绘制骨架 | 能跑起来，但规则、生成器、界面全塞在一个 1100 行的文件里 | 我确认了“关卡必须保证可解”这条硬性要求，让它把可解性检查做成生成流程的一部分 |
| 路径检测 | 同上 | 生成四个方向的整条射线扫描代码 `arrow_can_leave_from_occupied` | 远距离阻挡、自己的尾巴阻挡都能识别 | 我补了贴边阻挡、贴边朝外飞出两组边界用例，并核对“后方箭头不算阻挡” |
| 关卡生成 | 同上 | 生成带种子的 `build_level()` + 回溯校验 `solve_removal_order()` | 五关全部可解，箭头数 58/79/87/99/136 | 我要求难度逐关递增，并实际把五关都清了一遍确认没有死局 |
| 界面排错 | 同上 | 按字宽和按钮矩形重算文字锚点 | 顶栏文字不再互相压住 | 我逐张截图核对位置 |
| 测试与缺陷修复 | 同上 | 生成 68 项 `unittest`，用 SDL dummy 无窗口驱动真实界面 | 测出一个真实缺陷：超时判负后棋盘会卡死 | 我在 `shoot()` 的守卫里补上 `self.game.dead` |

### 过程一：先确定数据结构和“方向即向量”

**我的要求**：用 Python + Pygame 做一个“一箭又一箭”，箭头要有上下左右四个方向，
点击后能判断前方有没有阻挡。先把数据结构和主循环搭起来，能显示棋盘和箭头。

**AI 完成的工作**：给出了 `Arrow`（路径 + 方向 + 颜色）和 `GameState`（箭头列表 +
棋盘尺寸 + 生命值 + 状态标志）两个数据类，以及基于 `pygame` 的窗口创建、事件循环
和箭头绘制函数。

**实际效果**：第一版能运行，棋盘和箭头都正常显示。但四个方向在绘制和检测里各写了
一套 `if` 分支，后面加功能时很容易漏改其中一个方向。

**人工修改**：我要求把所有方向统一成 `(dx, dy)` 向量，路径检测、飞出路线、动画插值
全部只走一条代码路径。改完之后，后面加“飞出动画”时几乎没再动过检测逻辑。

### 过程二：把路径检测从“相邻一格”改成“整条射线”

**我的要求**：不要只看相邻的一格。箭头前面可能隔着好几个空格才有别的箭头，那也算
被挡住；另外箭头自己是蛇形的，如果尾巴正好在头前面，也应该算被自己挡住。

**AI 完成的工作**：把判断改成从 `头格 + 方向` 开始逐格推进，用 `while 0 <= col < cols
and 0 <= row < rows` 控制边界，循环里查 `occupied` 集合；同时补了一个
`get_block_info()` 返回阻挡者和阻挡位置，用来生成提示文字。

**实际效果**：隔着空格子的远距离阻挡能识别；位于箭头后方的、其他行列的箭头不会被
误判成阻挡；朝向棋盘外、循环体一次都不执行的边界情况也不会报越界错误。

**人工修改**：我把“自己的尾巴”这一条从隐式行为变成了显式提示——在 `get_block_info`
里用 `info["owner"] is arrow` 判断阻挡者是不是自己，提示文案区分“前方是自己的尾巴”
和“前方有箭身或尾巴挡住了”。另外补了两组边界测试（贴边阻挡、贴边朝外飞出）。

### 过程三：关卡改成程序化生成 + 每一步校验可解

**我的要求**：不要手写死关卡数据。要五关、难度递增、四个方向都要出现，而且**每一关
都必须能通关**，不能出现死局。

**AI 完成的工作**：写了带种子的 `build_level()`，分“基础箭头 → 延长箭身 → 填缝 →
兜底修复”四步生成，每一步插入一个箭头之后都调用 `solve_removal_order()` 做可解性
校验，不可解就立刻回滚这个箭头；还额外产出一份 `difficulty_profile()` 记录每一步有
多少个合法选择。

**实际效果**：五关全部通过可解性检查，箭头数量 58 / 79 / 87 / 99 / 136，逐关递增，
四方向齐全。同一个种子每次生成的布局完全一致，测试可以复现。

**人工修改**：第一版的难度曲线是乱的（第 3 关比第 4 关还多），我把每关的箭头数量
下限、箭身长度范围单独提成配置表 `LEVEL_CONFIGS`，然后逐关实际清了一遍，确认每一关
都存在合理的通关顺序、也没有“一眼看不出下一步”的关卡。

### 过程四：界面排版排错

**我的要求**：游戏界面顶栏的文字和按钮挤在一起了，帮我看看哪里重叠。

**AI 完成的工作**：把所有控件的矩形和文字锚点列出来做交叉比对，指出“关卡号”标题
居中在 `(296, 44)`，而“重玩”按钮占 `x 211~279`、`y 48~90`，两者在左上角区域重叠；
“剩余 N”的文字锚点在 `(214, 93)`，左半边压住了夜间模式开关的胶囊底图。

**实际效果**：把标题移到“重玩”按钮和倒计时之间的空档 `(338, 69)`，把“剩余 N”右移到
`(232, 93)`，两处挤压都消失了。截图逐张核对后确认没有引入新的重叠。

**人工修改**：我要求只动文字锚点，**不要移动任何可点击按钮的位置**，避免动到已经
验证过的点击热区。最终这次修改没有影响任何一项交互测试。

### 过程五：写测试测出一个真 bug

**我的要求**：按作业要求的 T01 ~ T06 写自动化测试，但不能只测纯函数，要用真实的
游戏对象跑完整流程；另外要覆盖边界和异常。

**AI 完成的工作**：写了 68 项 `unittest`，用 `SDL_VIDEODRIVER=dummy` 无窗口创建真实的
游戏对象，覆盖规则层、关卡生成、界面流程和命令行入口。测试全部通过后跑了第一轮，
但其中一个断言失败了。

**实际效果（关键）**：失败的是这一项——

```
test_clicking_the_board_after_a_timeout_does_not_freeze_the_game
AssertionError: True is not false : 已经判负的关卡不应该进入飞出状态
```

顺着这个失败查下去，找到的是一个真实缺陷。倒计时归零时 `update_game()` 会把
`game.dead` 置为 `True` 并弹出“时间到啦”弹窗；而此时如果用户关掉弹窗再点棋盘，
`shoot()` 的守卫条件只挡了 `moving / busy / complete`，**没有挡 `dead`**，于是箭头会
被置为“飞行中”：

```python
# 修改前
if not self.game or arrow.moving or self.game.busy or self.game.complete:
    return
```

问题是 `update_game()` 在 `dead` 之后会直接 return，永远不会再把动画推进到终点。
结果就是这支箭头永远停在飞行状态、`busy` 永远是 `True`，整个棋盘**永久卡死**，
点哪里都没有反应，只能靠“重玩”按钮救回来。

**人工修改**：我在守卫里补上了 `self.game.dead`（也就是本文第三节里那个版本的
`shoot()`），并保留这条回归测试，防止以后再把 `dead` 状态漏掉。这个 bug 靠手工点
很难稳定复现——要等 7 分钟倒计时跑完——这正是自动化测试的价值。

## 五、测试结果

### 1. 测试方法

用 Python 标准库 `unittest` 编写，共 **68 项**，分四层：

```powershell
python -m unittest discover -s tests -v
```

| 测试类 | 项数 | 覆盖内容 |
|---|---:|---|
| `ArrowRulesTests` | 19 | 路径检测、边界处理、失误扣减、计分、颜色与随机数工具 |
| `LevelGenerationTests` | 12 | 关卡结构合法性、四方向齐全、可解性、可复现性、克隆隔离 |
| `ArrowPuzzleFlowTests` | 36 | 用 SDL dummy 无窗口驱动真实界面：开始/点击/飞出/通关/失败/重开 |
| `CommandLineTests` | 1 | `--smoke-test` 命令行入口能正常退出 |

关键点：`ArrowPuzzleFlowTests` **不是**在测几个孤立函数，而是真的创建了游戏对象，
然后按真实鼠标路径点击（把格子中心换算成窗口坐标，再调用 `handle_mouse_down`），
所以它同时验证了“点击热区正确”和“规则正确”。

### 2. 手工测试（基本要求 T01 ~ T06）

| 编号 | 测试内容 | 预期结果 | 实际结果 | 是否通过 |
|---|---|---|---|---|
| T01 | 点击前方无阻挡的箭头 | 箭头飞出棋盘并消失 | 点击后进入飞出动画，动画结束后箭头从棋盘移除，剩余箭头数 -1，生命值不变 | 通过 |
| T02 | 点击前方有阻挡的箭头 | 箭头不消失，失误次数减 1 | 箭头留在原地且不进入动画，生命值 3 → 2，顶部出现“前方有箭身或尾巴挡住了 · 生命值 -1（剩余 2）” | 通过 |
| T03 | 点击位于边缘且朝向棋盘外的箭头 | 箭头正常消失，不发生越界错误 | 四个边缘方向各测一次，均正常飞出消失，无 `IndexError` / 越界异常 | 通过 |
| T04 | 消除本关全部箭头 | 显示通关并进入下一关 | 清空后弹出“通关成功！”，显示用时、剩余生命和得分，点“挑战下一关”进入第 2 关且棋盘重置 | 通过 |
| T05 | 失误次数耗尽 | 显示失败并允许重新开始 | 第 3 次误点后生命值归零，弹出“挑战失败”，点“重玩当前关卡”后生命恢复为 3、棋盘恢复初始布局 | 通过 |
| T06 | 游戏进行中重新开始 | 箭头布局和失误次数恢复 | 飞掉 3 支箭头并误点 1 次后点“重玩”，箭头布局与初始模板逐格一致，生命值回到 3，倒计时重置 | 通过 |

### 3. 扩展测试

| 编号 | 内容 | 结果 |
|---|---|---|
| T07 | 通关得分始终落在 0 ~ 100，且随时间、失误单调下降 | 通过 |
| T08 | 五个关卡四种方向（上下左右）全部出现 | 通过 |
| T09 | 阻挡判断覆盖“隔空阻挡 / 后方不算阻挡 / 其他行列不算阻挡” | 通过 |
| T10 | 一支箭头被自己的尾巴挡住时判定为阻挡 | 通过 |
| T11 | 射线正好停在棋盘边界上时仍然判定为阻挡 | 通过 |
| T12 | 点击空白格不扣生命、不触发动画 | 通过 |
| T13 | 箭头飞出动画期间不能重复点击，也不能点其他箭头 | 通过 |
| T14 | 关卡生成可复现：同一种子两次生成布局完全一致 | 通过 |
| T15 | 五关箭头不重叠、路径连续、只在单一轴上走、头方向与末段一致 | 通过 |
| T16 | 贪心地点“当前可飞出”的箭头，五关都能清空（即都可通关） | 通过 |
| T17 | `clone_level` 深拷贝，改克隆体不影响模板关卡 | 通过 |
| T18 | 通关写入历史得分，文件损坏时返回空列表而不崩溃 | 通过 |
| T19 | 退出到主界面、关闭弹窗、Esc 关弹窗等界面状态切换 | 通过 |
| T20 | 缩放、提示、辅助线、日夜模式的边界行为 | 通过 |
| T21 | **回归**：超时判负后点击棋盘不会进入飞出状态（本次修复的缺陷） | 通过（修复后） |

### 4. 测试中发现并修复的缺陷

| 缺陷 | 表现 | 根因 | 修复 |
|---|---|---|---|
| 超时判负后棋盘永久卡死 | 倒计时归零、关掉“时间到啦”弹窗后，点击箭头会让箭头进入“飞行中”状态，但动画永远不会结束，此后 `busy` 恒为 `True`，棋盘再也点不动 | `shoot()` 的守卫条件漏了 `self.game.dead`，而 `update_game()` 在 `dead` 之后会提前 return，不再推进动画 | 在 `shoot()` 守卫中加入 `self.game.dead`，并新增回归测试 `test_clicking_the_board_after_a_timeout_does_not_freeze_the_game` |

这个缺陷手工测试很难稳定复现（要把 7 分钟倒计时耗完），是自动化测试发现的。
修复后 68 项测试全部通过，耗时约 9 秒。

### 5. 手动试玩

除自动化测试外，五个关卡都实际清空过一遍，确认：

- 每一关开局都有可选箭头，不会一上手就是死局；
- 难度确实逐关递增（箭头数量 58 → 79 → 87 → 99 → 136）；
- 提示功能给出的箭头一定可以飞出；
- 通关、失败、重玩、选关、返回主界面五条流程都能正常走通。

## 六、PSP 表格

| 任务 | 预估耗时（小时） | 实际耗时（小时） | 差异（小时） |
|---|---:|---:|---:|
| 需求分析与游戏设计 | 1.5 | 2.0 | +0.5 |
| Python 与图形库学习 | 2.0 | 1.5 | -0.5 |
| 游戏界面实现 | 3.0 | 5.0 | +2.0 |
| 路径与碰撞逻辑实现 | 2.5 | 3.0 | +0.5 |
| 关卡设计（生成器 + 可解性校验） | 2.0 | 3.5 | +1.5 |
| AIGC 辅助开发 | 2.0 | 2.5 | +0.5 |
| 测试与修改 | 2.0 | 4.0 | +2.0 |
| README 与博客撰写 | 2.0 | 2.5 | +0.5 |
| **合计** | **17.0** | **24.0** | **+7.0** |

时间主要超在三个地方：一是界面细节反复调（顶栏排版、飞出动画的节奏、日夜配色）；
二是关卡生成器为了保证“每关都可解”，需要反复试参数；三是写测试时又测出了那个
超时卡死的缺陷，多花了一轮排查和回归的时间。

## 七、心得体会

**1. 规则想清楚了，代码才写得动。**
这个游戏最难的不是画出箭头，而是把“能不能飞出”这件事描述准确。一开始我脑子里
的想法是“看看前面有没有东西”，听起来很简单，落到代码里才发现至少要回答三个问题：
看多远（整条路径到边界）、看谁（包括自己的尾巴）、看到棋盘外面怎么办（循环条件
本身就是边界判断）。把“方向统一成 `(dx, dy)`”之后，这四个方向从四套分支变成了一条
代码路径，后面加飞出动画时几乎没有再改过检测逻辑。先把数据结构定对，比先写功能重要。

**2. AIGC 明显加速了“写”，但基本没有替代“判断”。**
AI 很快就能给出一个能跑的 Pygame 版本，`pygame` 的 API 细节、绘制样板、测试骨架这些
琐碎工作省了很多时间。但真正决定项目质量的几个判断，AI 都不会主动替我做：箭头是
单格还是多格、要不要保证关卡可解、难度曲线怎么排、界面元素该挪哪个。它给的默认答案
往往“能跑但不对味”，需要我把要求说得足够具体，它才能产出能用的东西。所以我把
“每一关都必须能通关”这条写成了硬性要求，才换来生成器里那四步校验和兜底修复。

**3. 需求要说具体，AI 的输出质量才稳定。**
“做个一箭又一箭”只能换来一个通用 demo；“箭头前方整条射线都要检查，包括自己的尾巴，
并且要告诉我被谁挡住了”才能换来 `get_block_info`。我逐渐习惯把要求拆成“现象 + 期望
行为 + 反例”：比如把“后方箭头不算阻挡”“隔着空格子也算阻挡”一起说出来，AI 就不容易
只处理最显然的那种情况。

**4. 自动化测试最大的价值是发现“想不到”的路径。**
那个超时卡死的缺陷，靠手工点几乎测不出来——得等 7 分钟倒计时走完、再关掉弹窗、再点
棋盘。写测试的时候我只是顺手加了一条“已经判负之后还能不能点棋盘”，结果它直接失败，
把 `shoot()` 漏掉 `dead` 守卫的问题顶了出来。而且这条测试写完就留下了，以后重构也不会
再把 `dead` 状态漏掉。这让我第一次真切体会到“测试不是交作业用的，是给自己兜底的”。

**5. 关卡生成比手写关卡数据靠谱得多。**
我一开始想手写五关的箭头坐标，排到第三关就发现很难保证“看起来乱、实际有解”。
改成程序化生成 + 每一步做可解性校验之后，不仅五关全部保证可解，还能用
`difficulty_profile` 看每一步有多少个合法选择，用数据判断难度是不是合适。
把“正确性”交给程序去验证，而把“好不好玩”留给自己判断，这个分工很舒服。

**6. 关于 AI 生成代码的责任。**
作业里写了“AI 生成的代码进入项目后，由提交者本人负责”。这句话在调试那个卡死缺陷时
特别有体感：AI 写出的代码读起来很顺，`if not self.game or arrow.moving or self.game.busy
or self.game.complete` 看起来已经把各种状态都挡住了，漏掉一个 `dead` 完全不显眼。
如果不逐行读、不实际跑、不写测试，这个 bug 就会一直躺在代码里，最后变成助教提问时
我答不上来的那一行。所以我现在的做法是：AI 生成的每一段关键逻辑，我都要能说清它为什么
这么写、边界在哪里、以及怎么验证它是对的。

## 八、提交前检查清单

- [x] 项目代码、README、依赖清单、界面截图已提交到 GitHub；
- [x] 多次有意义的 Commit（功能、测试、修复、文档分开提交）；
- [x] 五个关卡都实际清空验证过，存在合理的通关顺序；
- [x] 68 项自动化测试全部通过；
- [x] `run_python_game.bat` 双击可运行，缺依赖时会自动安装；
- [x] 仓库中没有 `.venv`、`__pycache__`、密码、Cookie、Token 等敏感内容；
- [x] README 说明了项目名称、简介、开发环境、安装运行方法、操作说明和截图。
